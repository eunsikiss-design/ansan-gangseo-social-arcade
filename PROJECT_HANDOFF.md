# 통합사회 탐구 아케이드 프로젝트 인수인계

## 1. 프로젝트 개요

- 대상: 고등학교 1학년 통합사회 2 수업
- 형식: 세로형 모바일 학습 게임
- 현재 범위: 1단원 인권, ACT 1만 구현
- 플레이어 역할: 국가인권수호국에 임명된 신입 인권수호관
- 핵심 동료: AI 탐구 파트너 아리, 인권수호관 해온
- 논쟁형 라이벌: ZERO
- 현재 기준 커밋: `b052a3b Add illustrated game icon system`
- 공개 범위: 현재 비공개 유지

## 2. 현재 구현 상태

- 메인 허브와 게임 소개
- 캐릭터 및 세계관 소개
- ACT 1 미션 지도 M01~M06
- 사건 브리핑, 캐릭터 대화, 조사·탐색, 자료 상세보기
- Evidence 수집 및 권리 카드 획득
- 퍼즐·선택형 문제와 오답 재도전
- ZERO Challenge와 최종 판단
- Evidence를 선택해 판단 제출
- 미션 종료 전 획득 카드와 현재 순위 표시
- 탐구 아카데미 ROOM 1~3
- 개념 카드 뒤집기, 미니 퀴즈, 사례 적용, 단계별 글쓰기
- 작성한 탐구 글 저장·조회·수정
- 타이머, 개념 이해도, 정확도, 평균 풀이 시간
- `localStorage` 기반 새로고침 후 이어하기
- 상황별 캐릭터 표정 전환
- 모바일 세로 화면 대응

## 3. 주요 파일

- `app/page.tsx`: 화면 전환, 저장 상태, 미션·아카데미·통계 UI
- `app/globals.css`: 전체 모바일 게임 디자인과 반응형 스타일
- `src/game/content.ts`: ACT 1 미션과 학습 콘텐츠
- `src/game/types.ts`: 게임 데이터 타입
- `src/game/characters.ts`: 캐릭터 이미지·표정 매핑
- `src/game/audio.ts`: 효과음 처리
- `public/assets/characters/`: 상황별 캐릭터 PNG
- `public/assets/icons/`: 단원·공통주제·게임 아이콘 PNG
- `public/assets/sources/`: 수업 자료 기반 삽화
- `tests/rendered-html.test.mjs`: 렌더링 회귀 테스트

## 4. 캐릭터 이미지 원칙

- 대화 문장은 이미지에 합성하지 않고 HTML 텍스트로 별도 표시한다.
- 캐릭터는 투명 배경 PNG 자산을 사용한다.
- 표정은 상황 상태에 따라 코드로 전환한다.
- 현재 연결 자산:
  - 플레이어: 기본, 고민
  - 아리: 기본, 성공
  - 해온: 기본, 진지
  - ZERO: 기본, 도전
- 새 표정이 필요하면 기존 화풍·의상·얼굴 정체성을 유지한다.

## 5. 콘텐츠 및 UX 원칙

- 문제 유형을 단순 선택형으로만 구성하지 않는다.
- 쉬운 단계에는 초성·OX·연결형·카드형 문항을 충분히 배치한다.
- 단계가 올라갈수록 사례, 그래프, 교과서 삽화와 자료 분석 비중을 높인다.
- 서술형은 한 문장 완성에서 주장·근거 구분, 자료 분석 의견 쓰기로 성장시킨다.
- 오답 시 정답을 먼저 노출하지 않고 다시 도전할 수 있어야 한다.
- 권리 카드 획득 조건을 화면에 명확하게 보여 준다.
- 다음 미션 전 획득 카드와 순위를 보여 준다.
- 스토리, 캐릭터 반응, 표정 전환이 학습 흐름과 함께 나타나야 한다.
- 모바일 세로 화면에서 버튼, 글자, 이미지가 잘리거나 겹치지 않아야 한다.

## 6. 작업 제한

- 사용자가 요청하기 전에는 ACT 2 또는 새로운 단원을 추가하지 않는다.
- 기존 ACT 1 흐름을 전면 재작성하거나 정상 동작을 삭제하지 않는다.
- 기존 캐릭터와 아이콘 자산을 임의로 다른 스타일로 교체하지 않는다.
- 수업 자료의 의미를 바꾸거나 근거 없는 판례·통계를 추가하지 않는다.
- 변경 전 `git status`를 확인하고 사용자 변경을 보존한다.
- 기능 변경 후 최소한 lint, 렌더링 테스트, production build를 확인한다.

## 7. 실행 및 검증

필요 환경은 Node.js 22.13 이상이다.

```powershell
npm install
npm run dev
```

로컬 주소: `http://localhost:3000`

검증 명령:

```powershell
npm run lint
node --test tests/rendered-html.test.mjs
npm run build
```

## 8. 외부 원전 자료

프로젝트에 복사된 사용 자산은 `public/assets`에서 확인한다. 원본 교과서·평가 자료를 다시 분석해야 할 때는 사용자의 허락을 받고 다음 외부 폴더를 Antigravity 프로젝트에 추가한다.

```text
D:\2. 교과지도\3.통사\2026-2학기
```

외부 원본을 이동·수정·삭제하지 않는다. 필요한 자산만 프로젝트 내부로 복사하고 출처와 학습 목적을 기록한다.

## 9. 알려진 운영 상태

- 로컬 프로젝트가 현재 개발의 기준본이다.
- Git 저장소는 존재하지만 원격 저장소는 연결되어 있지 않다.
- 따라서 다른 컴퓨터에서 작업하려면 비공개 Git 원격 저장소 또는 별도의 안전한 복사본이 필요하다.
- 기존 Codex 대화 기록은 Antigravity로 자동 이전되지 않으므로 이 문서를 기준 맥락으로 사용한다.

