# Character assets

완성 일러스트는 캐릭터별 폴더에 `{characterId}_{expression}.webp` 형식으로 넣습니다.
현재 게임은 요청 표정 → 기본 표정 → 공통 실루엣 → CSS 캐릭터/이름 순서로 안전하게 대체합니다.

대상 폴더: `player/`, `ari/`, `haeon/`, `zero/`, `npc/`, `common/`.
